{
  "name": "nodejs-api-that-takes-two-inputs",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "This server can be used as an intermediary to cache HTTP responses from a given URL. It listens on port 3000 by default, but that can be changed by setting the PORT environment variable.",
  "dependencies": {
    "axios": "^1.3.5",
    "express": "^4.18.2",
    "express-rate-limit": "^6.7.0",
    "morgan": "^1.10.0"
  },
  "devDependencies": {}
}
